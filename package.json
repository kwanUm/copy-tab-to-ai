{
  "name": "copy-tab-to-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test --config=playwright.config.mjs",
    "test:extract": "playwright test tests/extract.spec.mjs --config=playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0"
  }
}
